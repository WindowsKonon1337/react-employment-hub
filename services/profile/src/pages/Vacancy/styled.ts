import { Container } from "@/components";
import styled from "styled-components";

export const VacanciesContainer = styled(Container)({
	alignItems: "center",
	display: "flex",
	flexDirection: "column",
	gap: 20,
});
