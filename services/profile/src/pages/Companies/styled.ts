import styled from "styled-components";

import { ContentBlock } from "@/components/ModalContainer/styled";

export const CompaniesContainer = styled(ContentBlock)({
	display: "flex",
	gap: 25,
	flexWrap: "wrap",
});
